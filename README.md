# Google Sheets n8n Workflow

This workflow automatically posts data to Google Sheets via API.

## How It Works
- Trigger: [Webhook / Schedule]
- Action: Posts data to Google Sheets
- (Optional) Further actions can be added

## Workflow File
`google-sheets-workflow.json` can be imported directly into n8n.
